import React from 'react';
import "./Header.css"

const Header = () => {
    return (
        <header className="main-header">
            <h1>Real Soccer</h1>
        </header>
    );
};

export default Header;